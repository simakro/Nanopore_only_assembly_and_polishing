PlasFlow has currently to be cloned manually in this directory (because of 65Mb size) and then .git 
removed in order to remove its repo status, otherwise it will be a Matrushka Situation.
PLasClass is OK, because it is smaller. Git repo already removed here.import tensorflow
